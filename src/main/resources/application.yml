spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # DB ?? ??? ????? ??

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    hibernate:
      ddl-auto: update

# Jwt Token ?? ??
jwt:
  token:
    access-token-duration: 1800   # 30? = 1800?
    # secret key? ????? ??