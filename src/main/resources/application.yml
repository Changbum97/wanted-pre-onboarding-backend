spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # DB ?? ??? ????? ??

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    hibernate:
      ddl-auto: update

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

# Jwt Token ??
jwt:
  token:
    access-token-duration: 1800   # 1800? = 30?
    # secret key? ????? ??
